apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app-backend.fullname" . }}-config
  labels:
    {{- include "app-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
