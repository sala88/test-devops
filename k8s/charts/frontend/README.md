# Frontend Helm Chart

## Overview
Deploys the Frontend application (Nginx/Next.js).

## Components
- Deployment (Configurable to DaemonSet via values)
- Service
- Ingress
- ConfigMap

## Configuration
See `values.yaml` for options.
